
[iid@master-node agenticai]$ conda env list

conda activate agentic311


conda activate pytorch_env

conda deactivate 

conda create -n pytorch_env python=3.11

$ conda activate pytorch_env

pip install torch==2.7.0 torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu

pip install torchviz       --tovisulalise the graph 


sudo dnf install graphviz

(pytorch_env) [iid@master-node demo-project-pytorch-tutorial]$ pip install jupyter notebook

conda info --envs

conda list 

pip list 


==============installing the kernell within project ==================

(pytorch_env) [iid@master-node demo-project-pytorch-tutorial]$ python -m ipykernel install --user --name pytorch_env --display-name "PyTorch Env (3.11)"


================================end the jupyter kernel installation ====================================================
======================creating the virtual environment in windo machine==============

1-create any  folder in any drive and go there and then run below command and you can install other library.you will get the myenv folder 

py -3.11 -m venv myenv   
 
myenv\Scripts\activate       # every time you work

python -m ipykernel install --user --name=your_env_name --display-name "Python (your_env_name)"